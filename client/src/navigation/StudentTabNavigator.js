import React from 'react';import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';import { Home, FileText, Building, Banknote } from 'lucide-react-native';import DashboardScreen from '../screens/DashboardScreen';import AgenciesScreen from '../screens/AgenciesScreen'; import InstitutionsScreen from '../screens/InstitutionsScreen';import HelbScreen from '../screens/HelbScreen';import KuccpsScreen from '../screens/KuccpsScreen';import { Award } from 'lucide-react-native';const Tab = createBottomTabNavigator();export default function StudentTabNavigator({ route }) {    const { token } = route.params;    return (        <Tab.Navigator            screenOptions={({ route }) => ({                headerShown: false,                tabBarActiveTintColor: '#1e3a8a',                tabBarInactiveTintColor: '#64748b',                tabBarStyle: {                    paddingBottom: 5,                    paddingTop: 5,                    height: 60,                    backgroundColor: '#ffffff',                    borderTopWidth: 1,                    borderTopColor: '#e2e8f0',                },                tabBarLabelStyle: {                    fontSize: 12,                    fontWeight: '600'                },                tabBarIcon: ({ color, size }) => {                    if (route.name === 'Home') {                        return <Home color={color} size={size} />;                    } else if (route.name === 'Agencies') {                        return <FileText color={color} size={size} />;                    } else if (route.name === 'Institutions') {                        return <Building color={color} size={size} />;                    } else if (route.name === 'Placement') {                        return <Award color={color} size={size} />;                    } else if (route.name === 'Loans') {                        return <Banknote color={color} size={size} />;                    }                },            })}        >            <Tab.Screen                 name="Home"                 component={DashboardScreen}                 initialParams={{ token }}            />            <Tab.Screen                 name="Agencies"                 component={AgenciesScreen}                 initialParams={{ token }}                options={{ title: 'Records' }}            />            <Tab.Screen                 name="Institutions"                 component={InstitutionsScreen}                 initialParams={{ token }}                options={{ title: 'Institutions' }}            />            <Tab.Screen                 name="Placement"                 component={KuccpsScreen}                 initialParams={{ token }}                options={{ title: 'KUCCPS' }}            />            <Tab.Screen                 name="Loans"                 component={HelbScreen}                 initialParams={{ token }}                options={{ title: 'HELB Loan' }}            />        </Tab.Navigator>    );}