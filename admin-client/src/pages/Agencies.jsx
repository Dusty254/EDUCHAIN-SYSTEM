import React, { useState, useEffect } from 'react';import axios from 'axios';import { Building2, Search, Activity, Mail, PlusCircle, Globe } from 'lucide-react';import { API_BASE_URL } from '../config';export default function Agencies() {  const [agencies, setAgencies] = useState([]);  const [institutions, setInstitutions] = useState([]);  const [loading, setLoading] = useState(true);  useEffect(() => {    fetchData();  }, []);  const fetchData = async () => {    try {      const [agenciesRes, institutionsRes] = await Promise.all([        axios.get(`${API_BASE_URL}/admin/agencies`),        axios.get(`${API_BASE_URL}/agencies/institutions`)      ]);           if (agenciesRes.data.success) {        setAgencies(agenciesRes.data.data);      }      if (institutionsRes.data.success) {        setInstitutions(institutionsRes.data.data);      }    } catch (error) {      console.error('Error fetching data:', error);    } finally {      setLoading(false);    }  };  return (    <div className="p-8">      <div className="flex justify-between items-center mb-8">        <div>          <h2 className="text-2xl font-bold text-gray-800 flex items-center">            <Building2 className="mr-3 text-blue-600" /> Registered Agencies          </h2>          <p className="text-gray-500">View all institutional partners emitting records to the blockchain.</p>        </div>                <div className="flex space-x-4">          <div className="relative">            <Search className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" size={18} />            <input               type="text"               placeholder="Search by Name..."               className="pl-10 pr-4 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"            />          </div>          <button className="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium shadow-sm transition-colors flex items-center space-x-2">            <PlusCircle size={18} />            <span>Onboard Agency</span>          </button>        </div>      </div>      {loading ? (        <div className="py-12 text-center text-gray-500">Loading agency data...</div>      ) : agencies.length === 0 ? (        <div className="py-12 text-center text-gray-500 bg-white border border-gray-100 rounded-xl">           No agencies registered yet.        </div>      ) : (        <>          {}          <div className="mb-10">            <h3 className="text-xl font-bold text-gray-800 mb-6 border-b border-gray-100 pb-3">Core National Agencies</h3>            <div className="grid grid-cols-1 md:grid-cols-3 gap-6">              {agencies.map((agency) => (                <AgencyCard key={agency._id || agency.agencyId} agency={agency} />              ))}            </div>          </div>          {}          <div className="mt-8 bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">            <div className="p-6 bg-gray-50 border-b border-gray-200">              <div className="flex items-center">                <div className="h-12 w-12 rounded-lg bg-indigo-100 text-indigo-600 flex items-center justify-center font-bold text-xl mr-4 uppercase">                  IN                </div>                <div>                  <h3 className="text-xl font-bold text-gray-800">Institutions</h3>                  <p className="text-sm text-gray-500">Universities and TVETs authorized to issue certificates</p>                </div>              </div>            </div>            <div className="p-6">              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">                {institutions.map((inst) => (                  <AgencyCard key={inst._id || inst.institutionId} agency={inst} />                ))}              </div>              {institutions.length === 0 && (                <div className="text-center text-gray-500 py-4">No institutions registered yet.</div>              )}            </div>          </div>        </>      )}    </div>  );}const AgencyCard = ({ agency }) => (  <div className="bg-white rounded-xl shadow-sm border border-gray-100 p-6 flex flex-col h-full hover:shadow-md transition-shadow">    <div className="flex justify-between items-start mb-4">      <div className="h-12 w-12 rounded-lg bg-blue-50 text-blue-600 flex items-center justify-center font-bold text-xl uppercase">        {agency.name.substring(0, 2)}      </div>      <span className={`px-2.5 py-1 text-xs font-medium rounded-full ${agency.isActive ? 'bg-emerald-100 text-emerald-700' : 'bg-red-100 text-red-700'}`}>        {agency.isActive ? 'Active' : 'Inactive'}      </span>    </div>       <h3 className="text-lg font-bold text-gray-800 mb-1">{agency.name}</h3>    <p className="text-sm text-gray-500 mb-4">{agency.type}</p>    <div className="mt-auto space-y-3 pt-4 border-t border-gray-100">      <div className="flex items-center text-sm text-gray-600">        <Mail size={16} className="mr-2 text-gray-400" /> {agency.contactEmail}      </div>      <div className="flex items-center text-sm text-gray-600">        <Globe size={16} className="mr-2 text-gray-400" /> {agency.agencyId}      </div>    </div>  </div>);